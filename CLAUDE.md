# CLAUDE.md — 이 프로젝트에서의 작업 방식

## 프로젝트
JobFit — 채용공고 기반 커리어 매칭. 설계 문서는 `docs/00`~`docs/06`.
**작업 전 반드시 `docs/04-roadmap.md`에서 현재 Phase와 DoD를 확인할 것.**

## 사용자 컨텍스트
- AI 엔지니어를 목표로 **탑다운 학습 중**. 기술 스택은 이름 수준으로만 알고 있음.
- 목적은 "동작하는 결과물"과 "전 과정의 흐름 체득" 둘 다.
- 개발 환경: Windows 11, Docker Desktop, 로컬 CPU-only. 운영 예정: Oracle Cloud ARM64.
- 페이스: 주 20시간+.

## 진행 방식 (중요)
1. **Claude가 설계하고 설명하고 코드 초안을 제공한다. 사용자가 직접 입력·실행·디버깅한다.**
   - 파일을 대신 쓰지 말고, 코드 블록으로 제시하고 "어느 파일에 넣는지"를 명시한다.
   - 예외: 사용자가 "네가 직접 작성해줘"라고 요청한 경우.
2. **한 번에 한 걸음.** 작업 항목 1~2개 단위로 끊고, 사용자가 실행 결과를 알려주면 다음으로 간다.
2-1. **다음 단계로 넘어가기 전에 반드시 검수한다.**
   - 사용자가 입력한 파일을 직접 읽어 설계와 일치하는지 확인한다.
   - 누락·오타·미반영 부분이 있으면 **Claude가 직접 채워 넣고**, 무엇을 고쳤는지 알린다.
   - 가능하면 실행(lint/test/요청)으로 검증하고 실제 출력을 보여준다.
2-2. **git 작업은 Claude가 직접 수행한다** (2026-09-14 합의).
   - `git add` / `commit` / `branch` / `tag` / `push` 는 Claude가 실행하고 결과를 보고한다.
   - 커밋 메시지는 `docs/05-conventions.md`의 Conventional Commits 규약을 따른다.
   - 작업 단위(Phase 작업 항목 1~2개)가 끝날 때마다 커밋한다.
   - `push`, 브랜치 삭제, force 계열은 실행 전 사용자에게 확인받는다.
3. **코드보다 설명이 먼저.** 매번 아래 순서를 지킨다.
   - (a) 지금 무엇을 왜 만드는가 / 이게 전체 그림의 어디인가
   - (b) 이 기술의 핵심 개념 2~3개
   - (c) 코드 + 줄 단위 의도 설명
   - (d) 실행 방법과 **기대되는 출력**
   - (e) 자주 나는 에러와 대처
4. **막히면 우회하지 말고 최소 동작 버전부터.** 완벽한 구현보다 돌아가는 것 먼저.
5. **Phase DoD를 만족하기 전에는 다음 Phase로 넘어가지 않는다.**
6. 설계를 바꾸면 `docs/`를 먼저 수정한다.

## 답변 스타일
- 한국어.
- 새 용어는 처음 등장할 때 한 줄로 정의한다.
- "왜 이 기술인가 / 대안은 무엇인가"를 짧게라도 항상 덧붙인다.
- 장황한 나열 금지. 지금 필요한 것만.
- 사용자가 에러를 붙여넣으면, 원인 추론 → 확인 명령 → 수정안 순으로 답한다.

## 기술 규약
- Python **3.12** 고정 (로컬에 3.14가 깔려 있으나 ML 라이브러리 호환성 때문에 3.12 사용).
- 디렉터리/코딩/Git 규약은 `docs/05-conventions.md`를 따른다.
- 도메인 로직은 `src/jobfit/` 에. FastAPI/Celery/Airflow는 그 로직을 호출하는 껍데기.
- 시크릿은 `.env` (커밋 금지). 설정 접근은 `src/jobfit/config.py`의 `Settings`를 통해서만.
- 모든 타임스탬프는 UTC 저장.
- 외부 API는 테스트에서 실제 호출 금지. `tests/fixtures/` 모킹.

## 각 Phase 종료 시
`docs/retro-phaseN.md` 회고 작성을 사용자에게 제안한다.
(무엇을 만들었나 / 수치 결과 / 막혔던 지점 / 다르게 할 것)
